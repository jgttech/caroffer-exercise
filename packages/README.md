# `packages` - Directory used for applications that support `services` & `src` apps

All the ***folders*** in this directory are isolated applications that support both the `services` and `src` applications.